{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "dotnet fable watch src -s --run vite",
    "build": "dotnet fable src -c Debug && vite build",
    "test": "dotnet run --project tests/tests.fsproj",
    "clean": "dotnet fable clean"
  },
  "devDependencies": {
    "vite": "^8.0.13"
  },
  "version": "1.0.0",
  "dependencies": {
    "virtual-dom": "^2.1.1"
  }
}
